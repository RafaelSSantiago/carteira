{
  "compilerOptions": {
    "target": "ES6" /* Versão do ECMAScript para a qual o código será transpilado */,
    "module": "commonjs" /* Sistema de módulos utilizado (ex: CommonJS, ESNext) */,
    "lib": ["ES6"] /* Bibliotecas a serem incluídas na compilação */,
    "outDir": "./dist" /* Pasta de saída para os arquivos compilados */,
    "rootDir": "./src" /* Diretório raiz do código fonte */,
    "strict": true /* Habilita todas as verificações estritas */,
    "esModuleInterop": true /* Permite importação padrão para módulos CommonJS */,
    "skipLibCheck": true /* Pula verificação de tipos em arquivos de declaração (.d.ts) */,
    "forceConsistentCasingInFileNames": true /* Garante que os arquivos sejam referenciados com a mesma capitalização */
  },
  "include": ["src/**/*"] /* Define quais arquivos/pastas serão incluídos na compilação */,
  "exclude": ["node_modules", "**/*.spec.ts"] /* Define quais arquivos/pastas serão excluídos da compilação */
}
